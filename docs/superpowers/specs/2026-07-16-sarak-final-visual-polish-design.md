# SARAK Final Visual Polish Design

## Scope

Apply the approved final visual polish across home, shared heroes, activities contact CTA, donation, gallery, and contact. Then upload the verified project to `https://github.com/thevrushabhjain/Sarak-Website.git`.

## Assets

Use the newly added public assets where appropriate:

- `/murti.png` for About hero.
- `/programs.png` for Programs hero.
- Choose the best available `DSC_*.JPG` and `DJI_0828.JPG` images for activities, donation cards, and gallery-related visuals.

## Home

Move the `सेवा` content block farther right on desktop so it aligns better with the page grid while keeping mobile unchanged.

Move the support-system carousel from Home to Contact above the footer.

Replace the existing three-card CTA section above the footer with one full-width donation-focused horizontal banner.

Change the Featured Activity circular arrow button hover background to `#f97216`.

## Shared Heroes

For all non-home pages, make the hero image fill the full right-side area on desktop with stronger `object-cover` behavior and less visible empty space.

For Gallery, increase title line spacing.

For Contact, keep `हमसे जुड़ें` on one line for desktop.

## Activities

Make the contact CTA more balanced on desktop by reducing empty space between the left content and contact cards.

## Donation

Remove the donation scheme select buttons. Add images to scheme cards and maintain text readability using overlays/contrast.

## Contact

Move support-system carousel to Contact above the footer. Rebalance the Direct Connect panel so it fills the left column height better beside the enquiry form.

## Verification

Update source verification for the key structural/class changes and run the source verification plus production build before pushing.
